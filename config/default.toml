# Maschine MK3 -- Linux userspace driver
#
# Everything here is live: save the file and the running driver picks it up.
# A file that fails to parse or validate is reported and ignored, so a typo
# never takes the controller down mid-session.
#
# Channels are 1-16 as written on a DAW's front panel.
# Run `mk3-learn buttons` to fill in the [button.*] section for your unit.

[general]
client_name = "Maschine MK3"
out_port = "Controller Out"
in_port = "Controller In"
# SCHED_FIFO priority for the input thread. 0 turns real-time scheduling off.
# Needs rtprio headroom -- on most distributions, membership of the `audio` group.
realtime_priority = 80
lock_memory = true
# Configuration web interface. Set gui_port = 0 to disable it.
gui_port = 8730
gui_bind = "127.0.0.1"

[display]
enabled = true
brightness = 80          # 0-100
contrast = 50            # 0-100
fps = 30                 # redraw ceiling
title = "MASCHINE MK3"

[leds]
enabled = true
button_idle = 10         # 0-127
button_active = 127
fps = 60

[pads]
channel = 10
# One note per pad, indexed by HID pad number.
#
# The device numbers pads in READING ORDER: HID pad 0 is the TOP-left pad, the
# one silkscreened 13. Confirmed on hardware. The default below is transposed
# so the bottom-left pad (silkscreen 1, HID 12) plays the lowest note, which is
# what drum mapping conventions expect.
#
#   HID  0  1  2  3   = top row,    silkscreen 13 14 15 16
#   HID  4  5  6  7   =             silkscreen  9 10 11 12
#   HID  8  9 10 11   =             silkscreen  5  6  7  8
#   HID 12 13 14 15   = bottom row, silkscreen  1  2  3  4
#
# For a plain ascending run in hardware order instead, use
# notes = [36, 37, 38, ... 51].
notes = [48, 49, 50, 51, 44, 45, 46, 47, 40, 41, 42, 43, 36, 37, 38, 39]
# linear | soft | hard | fixed
curve = "linear"
fixed_velocity = 100
# Raw hit value that maps to velocity 127.
#
# The pads report 12 bits but never use the range. Measured on a real MK3: a
# hard hit lands around 1950, ordinary playing sits between 200 and 1400. If
# you keep pinning at 127, raise this; if the pads feel weak, lower it.
velocity_max = 2000
# Raw pressure value that maps to aftertouch 127. Unlike strike velocity,
# pressure really does use the whole 12-bit range -- leaning hard on a pad
# reaches 4094.
aftertouch_max = 4095
# Shaping for pressure, separate from the strike curve above. Pressure is
# steeply non-linear: it loiters between 30 and 150 through most of the
# comfortable travel and only rockets to 4094 at the very end. "linear" would
# leave aftertouch near zero for almost the whole gesture, so "soft" is the
# default. linear | soft | hard
aftertouch_curve = "soft"
# Ignore hits below this raw 12-bit value (0-4095). Raise it if a hard hit on
# one pad makes a neighbour whisper.
threshold = 0
# off | poly | channel
aftertouch = "poly"
aftertouch_floor = 64
# Send a note when a finger rests on a pad without striking it.
press_sends_note = false
# Palette indices; see `mk3-learn palette` for the device's built-in ramp.
# Idle pad colour. This is what the pads glow when nothing is being played --
# set idle_level = 0 if you would rather they stayed dark.
idle_colour = 11         # blue
active_colour = 17       # white
idle_level = 1           # 0 off, 1 dim, 2 normal, 3 bright
# LED slot of pad 0. The 103 slots are 62 button slots, then the 25 touch strip
# LEDs, then the 16 pads -- so the pads start at 87. See docs/hardware-map.md.
led_base = 87

[knobs]
channel = 1
ccs = [16, 17, 18, 19, 20, 21, 22, 23]
# absolute | relative
mode = "absolute"
# twos | bin-offset | sign-bit  (only used when mode = "relative")
relative_format = "twos"
# Send 14-bit values as cc and cc+32. Requires every cc below 32.
high_resolution = false
# jump | pickup
pickup = "jump"
# Ignore movements smaller than this many raw units (knobs read 0-999).
deadband = 2

[encoder]
channel = 1
cc = 24
mode = "relative"
relative_format = "twos"
step = 1

[touchstrip]
enabled = true
channel = 1
cc = 1
# Which analog field the strip reports on. Field 1 on the units tested. Run
# `mk3-learn watch` and slide a finger to check; if a different `analog N` line
# moves, put that number here.
#
# Field 0 is a free-running counter, not a control. It advances on every report
# and the driver refuses to use it as a source.
source = 1
# Value the strip reads at full deflection. It is 1024, not the pads' 4095.
max = 1024
# Light the strip's own LEDs to follow the finger.
leds = true
led_base = 62
led_count = 25
# Fill the meter from the high slot downwards. Slot order ascends along the
# strip; which physical end that is relative to your finger is unconfirmed, so
# flip this if the meter fills from the wrong end.
led_reversed = false
# Raw LED byte for a lit segment. The strip does not decode colour the way the
# pads do -- the pads' green comes out violet here -- so this is a raw value,
# not a palette index. Anything from 1 to 127; try a few.
led_value = 0x1f

[pedal]
enabled = false
channel = 1
ccs = [64, 67]
switch = true
switch_threshold = 2048

# ---------------------------------------------------------------------------
# Buttons -- confirmed on hardware
#
# `bit` is the button's index in the device's 80-bit field. All 72 below were
# read off a real MK3 by pressing each one; the remaining eight bits
# (1, 22, 23, 32, 54, 55, 62, 63) match no button on the panel and appear to be
# firmware padding. See docs/hardware-map.md.
#
# Every button sends its own CC on channel 16, numbered the same as its bit, so
# the mapping is predictable and collides with neither the pads (channel 10)
# nor the knobs (channel 1). Change any of them freely.
#
# `led` is the LED slot, -1 where it has not been identified yet; find more
# with `mk3-learn leds 16 62`. `led_colour` is -1 for a monochrome LED, or a
# palette index for a colour one -- the bank is mixed, and a colour LED reads
# its byte as (palette << 2) | level rather than as brightness.
#
# midi:     none | note CH N | cc CH N [VALUE] | pc CH N | start | stop | continue
# mode:     momentary | toggle | trigger
# led_mode: follow (mirror the button) | midi (mirror the host) | always | off
# ---------------------------------------------------------------------------

[button.jogwheel-push]
bit = 0
led = -1
led_colour = -1
midi = "none"
mode = "momentary"
led_mode = "follow"

[button.jogwheel-tilt-up]
bit = 2
led = -1
led_colour = -1
midi = "none"
mode = "momentary"
led_mode = "follow"

[button.jogwheel-tilt-right]
bit = 3
led = -1
led_colour = -1
midi = "none"
mode = "momentary"
led_mode = "follow"

[button.jogwheel-tilt-down]
bit = 4
led = -1
led_colour = -1
midi = "none"
mode = "momentary"
led_mode = "follow"

[button.jogwheel-tilt-left]
bit = 5
led = -1
led_colour = -1
midi = "none"
mode = "momentary"
led_mode = "follow"

[button.shift]
bit = 6
led = -1
led_colour = -1
midi = "cc 16 6"
mode = "momentary"
led_mode = "follow"

[button.screen-8]
bit = 7
led = 19
led_colour = -1
midi = "cc 16 7"
mode = "momentary"
led_mode = "follow"

[button.a]
bit = 8
led = -1
led_colour = 17
midi = "cc 16 8"
mode = "momentary"
led_mode = "follow"

[button.b]
bit = 9
led = -1
led_colour = 17
midi = "cc 16 9"
mode = "momentary"
led_mode = "follow"

[button.c]
bit = 10
led = -1
led_colour = 17
midi = "cc 16 10"
mode = "momentary"
led_mode = "follow"

[button.d]
bit = 11
led = -1
led_colour = 17
midi = "cc 16 11"
mode = "momentary"
led_mode = "follow"

[button.e]
bit = 12
led = -1
led_colour = 17
midi = "cc 16 12"
mode = "momentary"
led_mode = "follow"

[button.f]
bit = 13
led = -1
led_colour = 17
midi = "cc 16 13"
mode = "momentary"
led_mode = "follow"

[button.g]
bit = 14
led = -1
led_colour = 17
midi = "cc 16 14"
mode = "momentary"
led_mode = "follow"

[button.h]
bit = 15
led = -1
led_colour = 17
midi = "cc 16 15"
mode = "momentary"
led_mode = "follow"

[button.notes]
bit = 16
led = -1
led_colour = -1
midi = "cc 16 16"
mode = "momentary"
led_mode = "follow"

[button.volume]
bit = 17
led = -1
led_colour = -1
midi = "cc 16 17"
mode = "momentary"
led_mode = "follow"

[button.swing]
bit = 18
led = -1
led_colour = -1
midi = "cc 16 18"
mode = "momentary"
led_mode = "follow"

[button.tempo]
bit = 19
led = -1
led_colour = -1
midi = "cc 16 19"
mode = "momentary"
led_mode = "follow"

[button.note-repeat]
bit = 20
led = -1
led_colour = -1
midi = "none"
mode = "momentary"
led_mode = "follow"

[button.lock]
bit = 21
led = -1
led_colour = -1
midi = "cc 16 21"
mode = "momentary"
led_mode = "follow"

[button.pad-mode]
bit = 24
led = -1
led_colour = -1
midi = "cc 16 24"
mode = "momentary"
led_mode = "follow"

[button.keyboard]
bit = 25
led = -1
led_colour = -1
midi = "cc 16 25"
mode = "momentary"
led_mode = "follow"

[button.chords]
bit = 26
led = -1
led_colour = -1
midi = "cc 16 26"
mode = "momentary"
led_mode = "follow"

[button.step]
bit = 27
led = -1
led_colour = -1
midi = "cc 16 27"
mode = "momentary"
led_mode = "follow"

[button.fixed-vel]
bit = 28
led = -1
led_colour = -1
midi = "cc 16 28"
mode = "momentary"
led_mode = "follow"

[button.scene]
bit = 29
led = -1
led_colour = -1
midi = "cc 16 29"
mode = "momentary"
led_mode = "follow"

[button.pattern]
bit = 30
led = -1
led_colour = -1
midi = "cc 16 30"
mode = "momentary"
led_mode = "follow"

[button.events]
bit = 31
led = -1
led_colour = -1
midi = "cc 16 31"
mode = "momentary"
led_mode = "follow"

[button.variation]
bit = 33
led = -1
led_colour = -1
midi = "cc 16 33"
mode = "momentary"
led_mode = "follow"

[button.duplicate]
bit = 34
led = -1
led_colour = -1
midi = "cc 16 34"
mode = "momentary"
led_mode = "follow"

[button.select]
bit = 35
led = -1
led_colour = -1
midi = "cc 16 35"
mode = "momentary"
led_mode = "follow"

[button.solo]
bit = 36
led = -1
led_colour = -1
midi = "cc 16 36"
mode = "momentary"
led_mode = "follow"

[button.mute]
bit = 37
led = -1
led_colour = -1
midi = "cc 16 37"
mode = "momentary"
led_mode = "follow"

[button.pitch]
bit = 38
led = -1
led_colour = -1
midi = "cc 16 38"
mode = "momentary"
led_mode = "follow"

[button.mod]
bit = 39
led = -1
led_colour = -1
midi = "cc 16 39"
mode = "momentary"
led_mode = "follow"

[button.perform]
bit = 40
led = -1
led_colour = -1
midi = "cc 16 40"
mode = "momentary"
led_mode = "follow"

[button.restart]
bit = 41
led = -1
led_colour = -1
midi = "cc 16 41"
mode = "momentary"
led_mode = "follow"

[button.erase]
bit = 42
led = -1
led_colour = -1
midi = "cc 16 42"
mode = "momentary"
led_mode = "follow"

[button.tap]
bit = 43
led = -1
led_colour = -1
midi = "cc 16 43"
mode = "momentary"
led_mode = "follow"

[button.follow]
bit = 44
led = -1
led_colour = -1
midi = "cc 16 44"
mode = "momentary"
led_mode = "follow"

[button.play]
bit = 45
led = -1
led_colour = 17
midi = "cc 16 45"
mode = "momentary"
led_mode = "follow"

[button.rec]
bit = 46
led = -1
led_colour = 17
midi = "cc 16 46"
mode = "momentary"
led_mode = "follow"

[button.stop]
bit = 47
led = -1
led_colour = -1
midi = "cc 16 47"
mode = "momentary"
led_mode = "follow"

[button.macro]
bit = 48
led = 11
led_colour = -1
midi = "cc 16 48"
mode = "momentary"
led_mode = "follow"

[button.settings]
bit = 49
led = 9
led_colour = -1
midi = "cc 16 49"
mode = "momentary"
led_mode = "follow"

[button.arrow-right]
bit = 50
led = 7
led_colour = -1
midi = "cc 16 50"
mode = "momentary"
led_mode = "follow"

[button.sampling]
bit = 51
led = 5
led_colour = 17
midi = "cc 16 51"
mode = "momentary"
led_mode = "follow"

[button.mixer]
bit = 52
led = 3
led_colour = -1
midi = "cc 16 52"
mode = "momentary"
led_mode = "follow"

[button.plug-in]
bit = 53
led = 1
led_colour = -1
midi = "cc 16 53"
mode = "momentary"
led_mode = "follow"

[button.channel-midi]
bit = 56
led = 0
led_colour = -1
midi = "cc 16 56"
mode = "momentary"
led_mode = "follow"

[button.arranger]
bit = 57
led = 2
led_colour = -1
midi = "cc 16 57"
mode = "momentary"
led_mode = "follow"

[button.browser]
bit = 58
led = 4
led_colour = -1
midi = "cc 16 58"
mode = "momentary"
led_mode = "follow"

[button.arrow-left]
bit = 59
led = 6
led_colour = -1
midi = "cc 16 59"
mode = "momentary"
led_mode = "follow"

[button.file]
bit = 60
led = 8
led_colour = -1
midi = "cc 16 60"
mode = "momentary"
led_mode = "follow"

[button.auto]
bit = 61
led = 10
led_colour = -1
midi = "cc 16 61"
mode = "momentary"
led_mode = "follow"

[button.screen-1]
bit = 64
led = 12
led_colour = -1
midi = "cc 16 64"
mode = "momentary"
led_mode = "follow"

[button.screen-2]
bit = 65
led = 13
led_colour = -1
midi = "cc 16 65"
mode = "momentary"
led_mode = "follow"

[button.screen-3]
bit = 66
led = 14
led_colour = -1
midi = "cc 16 66"
mode = "momentary"
led_mode = "follow"

[button.screen-4]
bit = 67
led = 15
led_colour = -1
midi = "cc 16 67"
mode = "momentary"
led_mode = "follow"

[button.screen-5]
bit = 68
led = 16
led_colour = -1
midi = "cc 16 68"
mode = "momentary"
led_mode = "follow"

[button.screen-6]
bit = 69
led = 17
led_colour = -1
midi = "cc 16 69"
mode = "momentary"
led_mode = "follow"

[button.screen-7]
bit = 70
led = 18
led_colour = -1
midi = "cc 16 70"
mode = "momentary"
led_mode = "follow"

[button.jogwheel-touched]
bit = 71
led = -1
led_colour = -1
midi = "cc 16 71"
mode = "momentary"
led_mode = "follow"

[button.knob-8-touch]
bit = 72
led = -1
led_colour = -1
midi = "cc 16 72"
mode = "momentary"
led_mode = "follow"

[button.knob-7-touch]
bit = 73
led = -1
led_colour = -1
midi = "cc 16 73"
mode = "momentary"
led_mode = "follow"

[button.knob-6-touch]
bit = 74
led = -1
led_colour = -1
midi = "cc 16 74"
mode = "momentary"
led_mode = "follow"

[button.knob-5-touch]
bit = 75
led = -1
led_colour = -1
midi = "cc 16 75"
mode = "momentary"
led_mode = "follow"

[button.knob-4-touch]
bit = 76
led = -1
led_colour = -1
midi = "cc 16 76"
mode = "momentary"
led_mode = "follow"

[button.knob-3-touch]
bit = 77
led = -1
led_colour = -1
midi = "cc 16 77"
mode = "momentary"
led_mode = "follow"

[button.knob-2-touch]
bit = 78
led = -1
led_colour = -1
midi = "cc 16 78"
mode = "momentary"
led_mode = "follow"

[button.knob-1-touch]
bit = 79
led = -1
led_colour = -1
midi = "cc 16 79"
mode = "momentary"
led_mode = "follow"
